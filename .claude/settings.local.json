{
  "permissions": {
    "allow": [
      "Bash(find:*)",
      "Bash(del \"F:\\\\skillpal real estate\\\\backend\\\\debug-amenities.ts\")",
      "Bash(del \"F:\\\\skillpal real estate\\\\backend\\\\debug_output.txt\")",
      "Bash(del \"F:\\\\skillpal real estate\\\\backend\\\\debug_output_2.txt\")",
      "Bash(del \"F:\\\\skillpal real estate\\\\backend\\\\check-requests.js\")",
      "Bash(del \"F:\\\\skillpal real estate\\\\backend\\\\verify-fix.js\")",
      "Bash(del \"F:\\\\skillpal real estate\\\\backend\\\\src\\\\check_orphans.ts\")",
      "Bash(del \"F:\\\\skillpal real estate\\\\backend\\\\src\\\\check_user_pkgs.ts\")",
      "Bash(del \"F:\\\\skillpal real estate\\\\backend\\\\src\\\\debug_dues.ts\")",
      "Bash(dir:*)",
      "Bash(wsl bash -c \"ps aux | grep -iE ''\\(miner|xmrig|minerd|cryptonight|monero|ethminer|cgminer|bfgminer|nbminer|t-rex|lolminer|nicehash|kworker|python3 -c|curl.*sh|wget.*sh\\)'' 2>/dev/null\")",
      "Bash(python3:*)",
      "Bash(\"F:\\\\skillpal real estate\\\\.github\\\\workflows\\\\deploy.yml\" << 'WORKFLOW_EOF'\nname: Deploy to Production\n\non:\n  push:\n    branches:\n      - main\n\njobs:\n  # ─────────────────────────────────────────────────────────────────────────\n  # JOB 1: Build the Next.js frontend on the GitHub Actions runner.\n  # We do this here because the runner has more RAM \\(7 GB\\) than many VPS\n  # plans, and it avoids running a memory-hungry Next.js build on the server\n  # while live traffic is being served.\n  # ─────────────────────────────────────────────────────────────────────────\n  build-frontend:\n    name: Build Next.js Frontend\n    runs-on: ubuntu-latest\n\n    steps:\n      - name: Checkout repository\n        uses: actions/checkout@v4\n\n      - name: Set up Node.js 20\n        uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'npm'\n          cache-dependency-path: frontend/package-lock.json\n\n      - name: Install frontend dependencies\n        working-directory: frontend\n        run: npm ci\n\n      - name: Build Next.js app\n        working-directory: frontend\n        env:\n          # Public env vars are baked into the Next.js bundle at build time.\n          # Add every NEXT_PUBLIC_* variable your app needs here.\n          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}\n          NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}\n        run: npm run build\n\n      # Upload the compiled .next directory so the deploy job can SCP it.\n      - name: Upload .next build artifact\n        uses: actions/upload-artifact@v4\n        with:\n          name: nextjs-build\n          # .next/cache can be hundreds of MB; exclude it to keep the\n          # artifact small. The cache is only useful for rebuilds on the\n          # same runner, not on the server.\n          path: |\n            frontend/.next\n            !frontend/.next/cache\n          retention-days: 1\n\n  # ─────────────────────────────────────────────────────────────────────────\n  # JOB 2: Deploy – runs only after the frontend build succeeds.\n  # Steps:\n  #   a. Download the .next artifact built above.\n  #   b. SCP the .next directory to the server.\n  #   c. SSH in, pull latest code, build the backend, install frontend\n  #      runtime deps, and restart PM2.\n  # ─────────────────────────────────────────────────────────────────────────\n  deploy:\n    name: Deploy to Server\n    runs-on: ubuntu-latest\n    needs: build-frontend   # wait for the build job to finish\n\n    steps:\n      - name: Checkout repository\n        uses: actions/checkout@v4\n\n      # ── Download the .next artifact produced by the build job ──────────\n      - name: Download .next build artifact\n        uses: actions/download-artifact@v4\n        with:\n          name: nextjs-build\n          # Restore into frontend/ so the directory layout mirrors the repo.\n          path: frontend\n\n      # ── SCP: upload the compiled frontend to the server ────────────────\n      # We copy only .next \\(not node_modules, source files, etc.\\) because\n      # the server already has the full repo checked out via git pull below.\n      - name: Upload .next build to server\n        uses: appleboy/scp-action@v0.1.7\n        with:\n          host: ${{ secrets.HOST }}\n          username: ${{ secrets.USERNAME }}\n          key: ${{ secrets.KEY }}\n          passphrase: ${{ secrets.PASSPHRASE }}\n          port: ${{ secrets.PORT }}\n          # source path is relative to the GitHub Actions workspace root\n          source: \"frontend/.next\"\n          # target is the parent directory on the server; scp will create\n          # /var/www/skillpal/frontend/.next automatically\n          target: \"/var/www/skillpal\"\n          # IMPORTANT: do NOT set rm: true here. That flag deletes the\n          # entire target directory before copying, which would wipe out\n          # /var/www/skillpal \\(including backend, git repo, etc.\\).\n\n      # ── SSH: pull code, build backend, install deps, restart PM2 ───────\n      - name: Deploy on Server via SSH\n        uses: appleboy/ssh-action@v1.0.3\n        env:\n          # Pass the PAT as an environment variable so git can authenticate\n          # against the private repo without storing credentials on disk.\n          GITHUB_PAT: ${{ secrets.GITHUB_PAT }}\n        with:\n          host: ${{ secrets.HOST }}\n          username: ${{ secrets.USERNAME }}\n          key: ${{ secrets.KEY }}\n          passphrase: ${{ secrets.PASSPHRASE }}\n          port: ${{ secrets.PORT }}\n          envs: GITHUB_PAT\n          # Exit immediately on any error so a failed step is not silently\n          # swallowed and PM2 is never restarted with a broken build.\n          script_stop: true\n          script: |\n            set -euo pipefail\n\n            echo \"========================================\"\n            echo \" Starting deployment – $\\(date -u\\)\"\n            echo \"========================================\"\n\n            cd /var/www/skillpal\n\n            # ── Git pull ─────────────────────────────────────────────────\n            echo \"[1/4] Pulling latest code from main...\"\n            # Inject the PAT into the remote URL so git can reach a private\n            # repo without an SSH deploy key configured on the server.\n            git remote set-url origin \\\\\n              https://${GITHUB_PAT}@github.com/designdigistratics-dotcom/listing-hub.git\n            git fetch origin main\n            git reset --hard origin/main\n\n            # ── Backend build ─────────────────────────────────────────────\n            echo \"[2/4] Building backend...\"\n            cd /var/www/skillpal/backend\n\n            # npm ci installs exact versions from package-lock.json.\n            # We need devDependencies \\(TypeScript, ts-node-dev\\) to compile,\n            # so we do NOT pass --omit=dev here.\n            npm ci\n\n            # Regenerate the Prisma client whenever schema.prisma changes.\n            npx prisma generate\n\n            # Compile TypeScript to dist/\n            npm run build\n\n            # ── Frontend runtime setup ────────────────────────────────────\n            # The compiled .next was already uploaded by the SCP step above.\n            # We only need to ensure production node_modules are present.\n            echo \"[3/4] Installing frontend production dependencies...\"\n            cd /var/www/skillpal/frontend\n            npm ci --omit=dev\n\n            # ── Restart PM2 ───────────────────────────────────────────────\n            echo \"[4/4] Restarting PM2 processes...\"\n            pm2 restart skillpal-backend\n            pm2 restart skillpal-frontend\n\n            # Save the PM2 process list so it survives a server reboot.\n            pm2 save\n\n            echo \"========================================\"\n            echo \" Deployment complete – $\\(date -u\\)\"\n            echo \"========================================\"\nWORKFLOW_EOF)",
      "Bash(\"F:\\\\skillpal real estate\\\\deploy\\\\deploy.sh\" << 'DEPLOY_EOF'\n#!/usr/bin/env bash\n# =============================================================================\n# deploy.sh – Production deployment script for Skillpal / ListingHub\n#\n# Usage \\(run as root on the server, or via sudo\\):\n#   bash /var/www/skillpal/deploy/deploy.sh\n#\n# The GitHub Actions workflow calls the SSH steps directly and does not\n# invoke this script, but it is kept here so you can trigger a manual\n# re-deploy from the server without going through GitHub Actions.\n# =============================================================================\n\nset -euo pipefail   # exit on error, unset variable, or pipe failure\n\nAPP_ROOT=\"/var/www/skillpal\"\nLOG_FILE=\"/var/log/skillpal-deploy.log\"\nTIMESTAMP=\"$\\(date -u '+%Y-%m-%d %H:%M:%S UTC'\\)\"\n\n# ── Logging helper ────────────────────────────────────────────────────────────\nlog\\(\\) {\n  echo \"[${TIMESTAMP}] $*\" | tee -a \"$LOG_FILE\"\n}\n\nlog \"========================================================\"\nlog \" Manual deployment started\"\nlog \"========================================================\"\n\n# ── 1. Pull latest code ───────────────────────────────────────────────────────\nlog \"[1/4] Pulling latest code from origin/main...\"\ncd \"$APP_ROOT\"\n\n# If you have a GitHub PAT stored in an env file, source it before running\n# this script:  source /etc/skillpal/secrets && bash deploy/deploy.sh\nif [[ -n \"${GITHUB_PAT:-}\" ]]; then\n  git remote set-url origin \\\\\n    \"https://${GITHUB_PAT}@github.com/designdigistratics-dotcom/listing-hub.git\"\nfi\n\ngit fetch origin main\ngit reset --hard origin/main\n\n# ── 2. Build backend ──────────────────────────────────────────────────────────\nlog \"[2/4] Building backend \\(TypeScript -> dist/\\)...\"\ncd \"$APP_ROOT/backend\"\n\n# Install all deps including devDependencies \\(needed for tsc\\)\nnpm ci\n\n# Regenerate Prisma client in case schema changed\nnpx prisma generate\n\n# Compile TypeScript\nnpm run build\n\n# ── 3. Build frontend ─────────────────────────────────────────────────────────\nlog \"[3/4] Building frontend \\(Next.js\\)...\"\ncd \"$APP_ROOT/frontend\"\n\n# Install all deps \\(next build needs them\\)\nnpm ci\n\n# Build the Next.js app.\n# NEXT_PUBLIC_* variables must be set in the shell environment before\n# running this script if you are doing a full local build on the server.\nnpm run build\n\n# ── 4. Restart PM2 ────────────────────────────────────────────────────────────\nlog \"[4/4] Restarting PM2 processes...\"\n\npm2 restart skillpal-backend\npm2 restart skillpal-frontend\n\n# Persist the process list so PM2 restores it after a system reboot\npm2 save\n\nlog \"========================================================\"\nlog \" Deployment complete\"\nlog \"========================================================\"\nDEPLOY_EOF)",
      "Bash(\"F:\\\\skillpal real estate\\\\DEPLOYMENT.md\" << 'DOC_EOF'\n# Deployment Guide\n\nThis document explains the full CI/CD setup for the Skillpal / ListingHub\napplication \\(Node.js/Express backend + Next.js 15 frontend\\) deployed to a\nDigitalOcean Droplet at **143.244.185.51**.\n\n---\n\n## Architecture Overview\n\n```\nPush to main\n     │\n     ▼\nGitHub Actions\n  ├─ Job 1: build-frontend  \\(runs on GitHub runner – 7 GB RAM\\)\n  │    └─ npm ci + next build → uploads .next artifact\n  │\n  └─ Job 2: deploy  \\(runs after build-frontend succeeds\\)\n       ├─ Downloads .next artifact\n       ├─ SCP: copies .next to /var/www/skillpal/frontend/.next\n       └─ SSH:\n            ├─ git pull origin main\n            ├─ backend: npm ci + prisma generate + tsc\n            ├─ frontend: npm ci --omit=dev  \\(build already done above\\)\n            └─ pm2 restart skillpal-backend + skillpal-frontend\n```\n\n---\n\n## GitHub Secrets Required\n\nGo to your repository on GitHub:\n**Settings → Secrets and variables → Actions → New repository secret**\n\n| Secret name                  | Value                                                      |\n|------------------------------|------------------------------------------------------------|\n| `HOST`                       | `143.244.185.51`                                           |\n| `USERNAME`                   | `root`                                                     |\n| `PORT`                       | `22`                                                       |\n| `KEY`                        | Full content of the **private** SSH key \\(see below\\)        |\n| `PASSPHRASE`                 | Passphrase for the SSH key, or leave blank if none         |\n| `GITHUB_PAT`                 | GitHub Personal Access Token with `repo` scope             |\n| `NEXT_PUBLIC_API_URL`        | e.g. `https://api.yourdomain.com`                          |\n| `NEXT_PUBLIC_GOOGLE_CLIENT_ID` | Your Google OAuth client ID                              |\n\n---\n\n## Step-by-Step: Add the SSH Private Key Secret\n\n### On Windows \\(your machine\\)\n\n1. Open **PowerShell** \\(or Git Bash\\) and run:\n\n   ```powershell\n   Get-Content \"C:\\\\Users\\\\Nitin\\\\Desktop\\\\skillpal_ssh_key\" | Set-Clipboard\n   ```\n\n   This copies the entire private key to your clipboard.\n\n2. Go to GitHub → your repo → **Settings → Secrets and variables → Actions**.\n\n3. Click **New repository secret**.\n\n4. Name: `KEY`\n\n5. Value: paste from clipboard \\(Ctrl+V\\).\n\n   The value should look like:\n   ```\n   -----BEGIN OPENSSH PRIVATE KEY-----\n   b3BlbnNzaC1rZXktdjEAAAAA...\n   -----END OPENSSH PRIVATE KEY-----\n   ```\n   Make sure the entire key including the `-----BEGIN` and `-----END` lines\n   is included, with no trailing spaces.\n\n6. Click **Add secret**.\n\n> **Security note:** Never commit the private key file to git.\n> Never share it in Slack, email, or any other channel.\n\n---\n\n## Step-by-Step: Create a GitHub Personal Access Token \\(PAT\\)\n\nThe PAT is needed because the repo is **private**. The SSH action on the\nserver uses the PAT to authenticate `git pull`.\n\n1. Go to **GitHub → Settings \\(your account, not the repo\\) →\n   Developer settings → Personal access tokens → Tokens \\(classic\\)**.\n\n2. Click **Generate new token \\(classic\\)**.\n\n3. Note / name: `skillpal-deploy`\n\n4. Expiration: set a reasonable expiry \\(e.g. 1 year\\) and calendar a reminder\n   to rotate it before it expires.\n\n5. Scopes: tick **repo** \\(full control of private repositories\\).\n\n6. Click **Generate token** and immediately copy the token value.\n\n7. Add it as a GitHub secret named `GITHUB_PAT` \\(see table above\\).\n\n---\n\n## Step-by-Step: Add All Secrets to GitHub\n\n1. Navigate to: `https://github.com/designdigistratics-dotcom/listing-hub/settings/secrets/actions`\n\n2. For each row in the table at the top of this file, click\n   **New repository secret**, fill in the name and value, and save.\n\n3. Verify the secrets list shows all 8 secrets before triggering a deploy.\n\n---\n\n## How the Server-Side Deploy Script Works\n\n`deploy/deploy.sh` is a standalone script you can run manually on the server\nwhen you need to re-deploy without going through GitHub Actions \\(e.g. after\nan emergency hotfix applied directly on the server\\).\n\n```bash\n# On the server, as root:\nsource /etc/skillpal/secrets    # loads GITHUB_PAT into the environment \\(optional\\)\nbash /var/www/skillpal/deploy/deploy.sh\n```\n\nLogs are appended to `/var/log/skillpal-deploy.log`.\n\n---\n\n## PM2 Process Names\n\nThe workflow targets these exact PM2 process names \\(defined in\n`backend/ecosystem.config.js`\\):\n\n| PM2 name            | Directory                        | Port |\n|---------------------|----------------------------------|------|\n| `skillpal-backend`  | `/var/www/skillpal/backend`      | 5000 |\n| `skillpal-frontend` | `/var/www/skillpal/frontend`     | 3000 |\n\n---\n\n## Verifying a Deployment\n\n### 1. Watch the GitHub Actions run\n\n- Go to the **Actions** tab of the repo.\n- Click the most recent **\"Deploy to Production\"** workflow run.\n- Expand each step to see its output.\n- Both jobs must show a green checkmark.\n\n### 2. Check PM2 on the server\n\n```bash\nssh root@143.244.185.51\npm2 list\npm2 logs skillpal-backend --lines 50\npm2 logs skillpal-frontend --lines 50\n```\n\n### 3. Hit the health endpoints\n\n```bash\n# Backend health check\ncurl -s https://api.yourdomain.com/health\n\n# Frontend \\(should return the HTML of the home page\\)\ncurl -sI https://yourdomain.com\n```\n\n### 4. Check the deployment log\n\n```bash\ntail -f /var/log/skillpal-deploy.log\n```\n\n---\n\n## Troubleshooting\n\n| Symptom                                        | Likely cause & fix                                                     |\n|------------------------------------------------|------------------------------------------------------------------------|\n| \"Permission denied \\(publickey\\)\"                | Wrong SSH key in `KEY` secret. Re-paste the entire private key.        |\n| \"Host key verification failed\"                 | First connection; run `ssh-keyscan 143.244.185.51` and add the result to the workflow's `known_hosts` \\(handled automatically by appleboy/ssh-action\\). |\n| `npm ci` fails with peer-dep errors            | Lock file out of sync. Run `npm install` locally, commit `package-lock.json`, and push. |\n| `next build` fails with \"out of memory\"        | Build runs on GitHub runner \\(7 GB\\) – should not happen. If it does, add `NODE_OPTIONS=--max-old-space-size=4096` to the build step env. |\n| PM2 process not found                          | PM2 name mismatch. Run `pm2 list` on the server and update `ecosystem.config.js` if the names differ. |\n| Frontend shows stale content after deploy      | The `.next` artifact upload \\(SCP step\\) failed. Check the \"Upload .next build to server\" step in the Actions log. |\n| `prisma generate` fails                        | Prisma CLI not in `node_modules`. Ensure `prisma` is in `devDependencies` in `backend/package.json` \\(it is\\). |\n\n---\n\n## Secret Rotation Checklist\n\nWhen rotating credentials \\(recommended every 90 days or when a team member\nleaves\\):\n\n- [ ] Generate a new SSH key pair, add the public key to the server's\n      `~/.ssh/authorized_keys`, update `KEY` \\(and `PASSPHRASE`\\) in GitHub\n      Secrets, then remove the old public key from the server.\n- [ ] Generate a new GitHub PAT, update `GITHUB_PAT` in GitHub Secrets,\n      then revoke the old PAT.\n- [ ] Update `NEXT_PUBLIC_*` secrets if API URLs or OAuth credentials change.\nDOC_EOF)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(git push:*)",
      "Bash(ssh:*)",
      "WebFetch(domain:www.virustotal.com)",
      "Bash(echo:*)",
      "Bash(curl:*)",
      "Bash(node -e:*)",
      "WebFetch(domain:www.wiz.io)",
      "WebFetch(domain:nvd.nist.gov)",
      "Bash(ssh -i topickx_secure root@168.144.28.70 \"cd /var/www/skillpal/topickx/frontend && node_modules/.bin/next --version\")",
      "Bash(findstr:*)",
      "Bash(Get-Content \"C:\\\\Users\\\\Nitin\\\\.claude\\\\projects\\\\F--skillpal-real-estate\\\\68810c19-4262-4d4c-8f81-ae19853169e4.jsonl\")",
      "Bash(Select-String -Pattern \"postgres|backup|dump|pg_dump|restore\")",
      "Bash(Select-Object -First 50)",
      "Bash(ssh -i /c/Users/Nitin/Desktop/topickx_ssh_key root@165.232.181.226:*)",
      "Bash(ls -la \"F:\\\\skillpal real estate\\\\deploy\"\" 2>/dev/null || echo \"Directory not found or inaccessible \")"
    ]
  }
}
